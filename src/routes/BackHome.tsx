import { Modal } from 'antd';

export default function BackHome() {
    // Modal.error({
    //     title: "Permission denied",
    //     okText: "Back home",
    // })
    // setTimeout(() => {
    window.location.href = "/"
    // }, 3000);
    return (
        <></>
    )
}